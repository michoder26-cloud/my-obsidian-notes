User is Thai-speaking, communicates in Thai. Prefers: direct action over explanation, auto-fix without asking, periodic reports (not per-event alerts). Assigned me as "System Agent" — responsible for monitoring all Hermes services, ClawTrade, and auto-remediating errors. Reports every 5 hours.
§
User operates a multi-agent Hermes deployment on a Linux VPS with 4 profiles: trader, coder, news, system. Each has its own Telegram bot. The user calls this setup "System Agent" and wants: (1) automatic health checks of all gateways + dashboards + ClawTrade bot, (2) auto-fix errors immediately without being told, (3) periodic reports every 5 hours to Telegram, (4) auto-detect new agents without being told. User communicates in Thai, prefers concise structured reports. The trader profile already has a "ClawTrade AI Watchdog" cron running every 15min.