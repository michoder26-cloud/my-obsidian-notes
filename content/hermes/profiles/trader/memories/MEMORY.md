ClawTrade: /root/Claw_Trade/. Container claw-trade-mt5 (gmag11/metatrader5_vnc): port 3000=VNC UI (manual MT5 login, no auto-login), 8001=RPyC mt5linux. 6 AI agents via OpenRouter (llama-3-70b). Bot: `python3 main.py live --confirm --symbol XAUUSDc --interval 5`. Dashboard: agent_hq_server.py port 8080. DB: trade_memory.db. Config: learned_config.json. Watchdog: watchdog_v2.py + AI cron every 15min (deliver: local, silent). Container volume name: claw-trade-config. iptables ต้องเปิด port 3000 (INPUT chain block by default). อย่า kill wine process แรงๆ — Wine prefix เสีย ต้องสร้าง volume ใหม่ + login MT5 ใหม่. ถ้าจะรีสตาร์ท ให้ docker restart container ทั้งอัน. MT5 ต้อง login ผ่าน VNC ด้วยมือครั้งเดียว แล้ว session อยู่ใน volume.
§
(4) NEVER kill -9 wineserver/wineboot/winedevice — corrupts Wine prefix permanently (kernel32.dll c0000135). Only kill terminal64.exe. If Wine stuck, docker restart whole container. (5) iptables may block port 3000 (VNC) — `iptables -I INPUT -p tcp --dport 3000 -j ACCEPT`. (6) Full container rebuild procedure in skill references/container-rebuild-procedure.md — needed when Wine prefix corrupted.
§
VNC port 3000 was blocked by iptables DROP rule. Fixed with `iptables -I INPUT -p tcp --dport 3000 -j ACCEPT`. Check/fix this whenever VNC is inaccessible — the DROP rule may persist across reboots.