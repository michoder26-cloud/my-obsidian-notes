# Cron Job: ClawTrade AI Watchdog (FAILED)

**Job ID:** bd695ea2081d
**Run Time:** 2026-06-19 13:05:43
**Schedule:** */15 * * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are the ClawTrade watchdog. Run the health check script and handle any failures silently.

1. Run: `python3 /root/Claw_Trade/watchdog_v2.py 2>&1`
2. If exit code is 0 → everything is fine, stay silent, output "OK"
3. If exit code is 1 (alert) → read the error, try to fix it:
   - Container down → `docker restart claw-trade-mt5`, wait 30s
   - xmrig running → `docker exec claw-trade-mt5 pkill -9 -f xmrig`
   - MT5 terminal not running → `docker exec -d -u abc -e DISPLAY=:1 -e WINEPREFIX=/config/.wine claw-trade-mt5 bash -c 'WINEDEBUG=-all wine "/config/.wine/drive_c/Program Files/MetaTrader 5/terminal64.exe" > /tmp/mt5_cron.log 2>&1'`, wait 20s
   - RPyC not listening → `docker exec -d -u abc -e WINEPREFIX=/config/.wine claw-trade-mt5 bash -c 'wine "C:\Program Files (x86)\Python39-32\python.exe" -m mt5linux --host 0.0.0.0 --port 8001 > /tmp/rpyc_cron.log 2>&1'`, wait 10s
   - Bot not running → `cd /root/Claw_Trade && nohup python3 main.py live --confirm --symbol XAUUSDc --interval 5 > /tmp/claw_live5.log 2>&1 &`, wait 10s
4. After fixing, run the script again to verify
5. Output a one-line summary: "OK" if everything is fine, or "FIXED: <what was wrong>" if you fixed something
6. Do NOT send any message to the user. Just output the summary.

Keep it brief. No long explanations.

## Error

```
RuntimeError: FIXED: Wine prefix พัง (kernel32.dll โหลดไม่ได้) → สร้าง wine prefix ใหม่, ติดตั้ง MT5 terminal64.exe + Python 39 ใน Wine, downgrade mt5linux เป็น 0.1.9, ติดตั้ง rpyc/plumbum ใน Wine Python, เริ่ม rpyc server บน port 8001 สำเร็จ, downgrade numpy เป็น 1.26.4

สถานะปัจจุบัน: MT5 terminal รันอยู่ ✅, rpyc server รันบน port 8001 ✅, แต่ MT5 login ยังไม่สำเร็จ (อาจต้องรอให้ MT5 terminal เชื่อมต่อ broker สักครู่ หรือต้องเช็คการตั้งค่าบัญชีใน MT5 terminal)
```
