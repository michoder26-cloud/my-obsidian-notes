# Cron Job: Gold Sniper Dashboard Watchdog

**Job ID:** 055c26dcbed9
**Run Time:** 2026-06-15 10:26:41
**Schedule:** once in 30m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

ตรวจสอบว่า Gold Sniper Dashboard server ที่ port 8080 รันอยู่หรือไม่

1. ตรวจสอบกระบวนการ: ps aux | grep dashboard_server
2. ตรวจสอบ HTTP: curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/
3. ถ้าไม่รัน (ไม่มี process หรือ HTTP ไมตอบกลับ 200):
   - รีสตาร์ท: cd /root/Claw_Trade && nohup python3 dashboard_server.py > /tmp/dash.log 2>&1 &
   - รอ 3 วิ
   - ตรวจสอบอีกครั้ง
   - ถ้ายังไม่รัน: แจ้งเตือน "⚠️ Dashboard ยังรันไม่ได้หลังจากพยายาม restart"
   - ถ้ารันได้: แจ้งเตือน "🔄 Dashboard ถูก restart อัตโนมัติแล้ว"
4. ถ้ารันปกติ: เงียบ ไม่ต้องแจ้งเตือนอะไร

## Response

[SILENT]
