# 🚀 คู่มือการติดตั้ง Boss Sniper บน VPS (Step-by-Step)

ยินดีด้วยครับบอส! ตอนนี้บอสพร้อมที่จะรันระบบเทรดอัตโนมัติ 24/7 แล้ว ทำตามขั้นตอนด้านล่างนี้เพื่อเริ่มรันระบบครับ:

---

## 🛠️ ขั้นตอนที่ 1: เตรียมเครื่อง VPS
1.  **Remote Desktop:** ใช้โปรแกรม Remote Desktop บนคอมพิวเตอร์ของบอส เชื่อมต่อเข้าไปที่ VPS โดยใช้ IP, Username, และ Password ที่ได้มาจากผู้ให้บริการ
2.  **ติดตั้ง Python:** โหลดและติดตั้ง **Python 3.10 หรือ 3.11** (ติ๊กถูกที่ "Add Python to PATH" ตอนติดตั้งด้วยนะครับ)
3.  **ติดตั้ง MT5:** โหลดและติดตั้งโปรแกรม **MetaTrader 5** ของ Broker ที่บอสใช้ และ Login เข้าพอร์ตให้เรียบร้อย

---

## 📦 ขั้นตอนที่ 2: เตรียมไฟล์ระบบ
1.  **แตกไฟล์:** นำไฟล์ `Boss_Sniper_ULTIMATE_Setup.zip` ไปวางบน Desktop ของ VPS แล้วกด Extract All
2.  **ตรวจสอบไฟล์ .env:** เปิดไฟล์ `.env` ในโฟลเดอร์บอท ดูว่า API Key และเลขพอร์ต MT5 ถูกต้องไหม

---

## ⚡ ขั้นตอนที่ 3: ติดตั้งไลบรารีที่จำเป็น
เปิดโปรแกรม **Command Prompt (CMD)** หรือ **PowerShell** ในโฟลเดอร์บอท แล้วพิมพ์คำสั่งนี้:
```bash
pip install pandas numpy yfinance pandas_ta MetaTrader5 anthropic python-dotenv
```

---

## 🚀 ขั้นตอนที่ 4: เริ่มรันบอทสไนเปอร์!
เมื่อทุกอย่างพร้อมแล้ว ให้พิมพ์คำสั่งนี้ใน CMD เพื่อเริ่มรันระบบเทรดจริง:
```bash
python main.py trading
```

---

## 🧐 การตรวจสอบการทำงาน
*   **Logs:** บอสสามารถดูการตัดสินใจของ Agent ได้ในหน้าจอ CMD เลยครับ
*   **MT5:** ตรวจสอบที่หน้าจอ MT5 ว่ามีออเดอร์ถูกส่งเข้าไปไหม
*   **Howtoclaw.md:** ถ้าอยากปรับความแม่นยำหรือเพิ่ม Lot ให้กลับไปอ่านคู่มือปรับจูนที่ผมเขียนไว้ให้ครับ

---

### 📢 คำแนะนำพิเศษ:
หากบอสเจอปัญหาหรือบอทไม่ทำงาน ให้เช็คว่าบอสกดปุ่ม **"Algo Trading"** ใน MT5 เป็นสีเขียวแล้วหรือยังนะครับ!

**ขอให้บอสร่ำรวยและพอร์ตเขียวขจีครับ!** 🏹🎯💰👑👑
